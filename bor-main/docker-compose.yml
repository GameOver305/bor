version: '3.8'

services:
  bot:
    build: .
    container_name: bor-discord-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - TZ=Asia/Riyadh
