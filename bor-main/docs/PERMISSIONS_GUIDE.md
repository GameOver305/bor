# 🔐 دليل نظام الصلاحيات والمالك

## 📋 نظرة عامة

تم إضافة نظام صلاحيات شامل للبوت يتيح للمالك التحكم الكامل في البوت وإدارة المشرفين والمراقبين مع إمكانية منح صلاحيات مخصصة.

---

## 👑 المالك (Owner)

### معرف المالك
**Discord ID:** `1376784524016619551`

### صلاحيات المالك
المالك لديه **جميع الصلاحيات** في البوت:
- ✅ الوصول الكامل لجميع الأوامر
- ✅ إضافة وإزالة المشرفين
- ✅ إضافة وإزالة المراقبين
- ✅ منح صلاحيات مخصصة
- ✅ إلغاء صلاحيات المستخدمين
- ✅ عرض قائمة جميع الصلاحيات
- ✅ التحقق من صلاحيات أي مستخدم

---

## 🎯 التسلسل الهرمي للصلاحيات

```
المالك (Owner) - ID: 1376784524016619551
    ↓ جميع الصلاحيات
المشرف (Admin)
    ↓ صلاحيات إدارية كاملة
المراقب (Moderator)
    ↓ صلاحيات مراقبة محدودة
المستخدم (User)
    ↓ صلاحيات أساسية
```

---

## 🛠️ أوامر إدارة الصلاحيات

جميع الأوامر التالية **متاحة للمالك فقط**.

### 1️⃣ تعيين مشرف

**الأمر:**
```
/perms set_admin @المستخدم notes:"ملاحظات اختيارية"
```

**مثال:**
```
/perms set_admin @أحمد notes:"مشرف موثوق"
```

**الصلاحيات الممنوحة:**
- إدارة جميع الحجوزات
- إدارة التحالفات
- عرض الإحصائيات
- تصدير البيانات
- إنشاء نسخ احتياطية
- الوصول للوحة الأدمن

---

### 2️⃣ تعيين مراقب

**الأمر:**
```
/perms set_moderator @المستخدم notes:"ملاحظات اختيارية"
```

**مثال:**
```
/perms set_moderator @سارة notes:"مساعد في الدعم"
```

**الصلاحيات الممنوحة:**
- إدارة الحجوزات
- عرض إحصائيات محدودة
- مساعدة المستخدمين

---

### 3️⃣ منح صلاحيات مخصصة

**الأمر:**
```
/perms grant @المستخدم
```

**طريقة الاستخدام:**
1. قم بتشغيل الأمر
2. اختر الصلاحيات من القائمة:
   - 📅 إدارة الحجوزات
   - 🤝 إدارة التحالفات
   - 📊 عرض الإحصائيات
   - 📥 تصدير البيانات
   - 💾 إنشاء نسخ احتياطية
3. اضغط "✅ تأكيد المنح"

**ملاحظة:** يجب أن يكون المستخدم مشرفاً أو مراقباً أولاً

---

### 4️⃣ إزالة صلاحيات مستخدم

**الأمر:**
```
/perms remove @المستخدم
```

**مثال:**
```
/perms remove @خالد
```

**التأثير:**
- إزالة جميع الصلاحيات الممنوحة
- حذف السجل من قاعدة البيانات
- إرسال إشعار للمستخدم

---

### 5️⃣ عرض قائمة الصلاحيات

**الأمر:**
```
/perms list
```

**العرض:**
- 👑 **المالك** - معلومات المالك
- 👨‍💼 **المشرفون** - قائمة جميع المشرفين
- ⭐ **المراقبون** - قائمة جميع المراقبين
- 🔑 **صلاحيات مخصصة** - قائمة الصلاحيات المخصصة

---

### 6️⃣ التحقق من صلاحيات مستخدم

**الأمر:**
```
/perms check @المستخدم
```

**مثال:**
```
/perms check @محمد
```

**العرض:**
- الدور الحالي (مشرف/مراقب/مستخدم)
- قائمة الصلاحيات المخصصة
- تاريخ المنح
- من منح الصلاحيات

---

## 📊 الصلاحيات المخصصة المتاحة

### 1. إدارة الحجوزات (`manage_bookings`)
- إدارة جميع الحجوزات
- إلغاء حجوزات المستخدمين
- تعديل الحجوزات

### 2. إدارة التحالفات (`manage_alliances`)
- إدارة جميع التحالفات
- حل التحالفات
- تعديل معلومات التحالفات

### 3. عرض الإحصائيات (`view_stats`)
- عرض إحصائيات البوت
- عرض تقارير مفصلة
- الوصول للوحة الإحصائيات

### 4. تصدير البيانات (`export_data`)
- تصدير البيانات إلى CSV
- تصدير البيانات إلى JSON
- تصدير السجلات

### 5. إنشاء نسخ احتياطية (`create_backups`)
- إنشاء نسخ احتياطية فورية
- جدولة النسخ الاحتياطية
- استعادة من نسخة احتياطية

---

## 🔒 الأمان والحماية

### حماية المالك
- ❌ لا يمكن إزالة صلاحيات المالك
- ❌ لا يمكن تعديل دور المالك
- ✅ المالك لديه صلاحيات دائمة

### سجل التدقيق
جميع التغييرات في الصلاحيات يتم تسجيلها في جدول `permissions_log`:
- من قام بالتغيير
- ما هو التغيير
- متى حدث التغيير
- السبب (إن وجد)

### إشعارات
عند تغيير الصلاحيات:
- ✅ يتم إرسال إشعار للمستخدم المتأثر
- ✅ يتم تسجيل العملية في السجلات
- ✅ يتم تحديث قاعدة البيانات فوراً

---

## 💡 أمثلة عملية

### مثال 1: إضافة مشرف جديد
```
الموقف: تريد إضافة مشرف جديد لمساعدتك في إدارة البوت

الخطوات:
1. /perms set_admin @أحمد notes:"مشرف فريق الدعم"
2. سيحصل أحمد على جميع صلاحيات المشرف
3. سيتلقى أحمد إشعاراً بالتعيين
```

### مثال 2: منح صلاحية محددة
```
الموقف: تريد السماح لمراقب بتصدير البيانات فقط

الخطوات:
1. تأكد أن المستخدم مراقب أو مشرف
2. /perms grant @سارة
3. اختر "📥 تصدير البيانات" من القائمة
4. اضغط "✅ تأكيد المنح"
```

### مثال 3: إزالة صلاحيات
```
الموقف: مستخدم لم يعد يعمل معك

الخطوات:
1. /perms remove @خالد
2. سيتم إزالة جميع صلاحياته
3. سيتلقى إشعاراً بالإزالة
```

---

## 🗄️ قاعدة البيانات

### جدول `bot_permissions`
يخزن معلومات الصلاحيات:
```sql
- discord_id: معرف المستخدم
- username: اسم المستخدم
- role: الدور (owner/admin/moderator/user)
- permissions: الصلاحيات المخصصة (JSON)
- granted_by: من منح الصلاحيات
- granted_at: تاريخ المنح
- notes: ملاحظات
```

### جدول `permissions_log`
يخزن سجل التغييرات:
```sql
- action: نوع العملية
- target_discord_id: معرف المستخدم المستهدف
- performed_by: من قام بالعملية
- old_role: الدور القديم
- new_role: الدور الجديد
- permissions_changed: الصلاحيات المتغيرة
- created_at: وقت العملية
```

---

## ⚙️ الإعدادات

### في ملف `.env`
```env
# Owner Configuration
OWNER_ID=1376784524016619551
```

### في `config.py`
```python
OWNER_ID: int = int(os.getenv('OWNER_ID', 1376784524016619551))
```

---

## 🔧 استكشاف الأخطاء

### المشكلة: "هذا الأمر متاح للمالك فقط"
**الحل:** تأكد أن معرفك هو `1376784524016619551`

### المشكلة: لا يمكن منح صلاحيات مخصصة
**الحل:** تأكد أن المستخدم مشرف أو مراقب أولاً

### المشكلة: الصلاحيات لا تعمل
**الحل:** 
1. تأكد من تشغيل الترقية: `python database/migrations/add_permissions_system.py`
2. أعد تشغيل البوت

---

## 📚 الأسئلة الشائعة

### س: هل يمكن تغيير معرف المالك؟
**ج:** نعم، من خلال تعديل متغير `OWNER_ID` في ملف `.env`

### س: كم عدد المشرفين المسموح به؟
**ج:** لا يوجد حد، يمكنك إضافة عدد غير محدود من المشرفين

### س: هل يمكن للمشرف إضافة مشرفين آخرين؟
**ج:** لا، فقط المالك يمكنه إضافة مشرفين

### س: ماذا يحدث إذا تم إزالة صلاحيات مشرف؟
**ج:** يتم حذف سجله من قاعدة البيانات ويعود إلى مستخدم عادي

### س: هل يمكن استعادة الصلاحيات المحذوفة؟
**ج:** نعم، السجلات موجودة في `permissions_log` ويمكن للمالك إعادة منحها

---

## 🎯 الخلاصة

نظام الصلاحيات الجديد يوفر:
- ✅ تحكم كامل للمالك
- ✅ إدارة مرنة للمشرفين والمراقبين
- ✅ صلاحيات مخصصة دقيقة
- ✅ سجل تدقيق شامل
- ✅ أمان وحماية قوية

**المالك (ID: 1376784524016619551) الآن لديه السيطرة الكاملة على البوت!** 👑

---

**تاريخ الإصدار:** 2026-02-13  
**الإصدار:** 2.1.0
