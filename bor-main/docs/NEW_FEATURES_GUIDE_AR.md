# 🎮 نظام البوت الاحترافي - التحديث الشامل v2.0

## 📋 ملخص التحديثات الجديدة

تم تطوير البوت بشكل شامل ليصبح نظاماً احترافياً متكاملاً مع واجهة أزرار تفاعلية، نظام تحالفات متقدم، لوحة تحكم للأدمن، ودعم ثنائي اللغة.

---

## ✨ الميزات الجديدة

### 1️⃣ نظام القائمة الرئيسية بالأزرار (Main Menu System)

**الأوامر:**
- `/start` - فتح القائمة الرئيسية
- `/menu` - فتح القائمة الرئيسية

**المزايا:**
- ✅ واجهة أزرار تفاعلية احترافية
- ✅ أزرار منظمة في صفوف واضحة
- ✅ تنقل سهل بين الأقسام
- ✅ أيقونات Emojis مناسبة لكل وظيفة

**الأزرار المتوفرة:**
```
📅 حجز موعد | 📋 مواعيدي | 📊 جدول المواعيد
📈 إحصائياتي | 🏆 المتصدرون | 🤝 التحالفات
⚙️ الإدارة (للأدمن فقط) | 🌐 اللغة | ❓ المساعدة
```

---

### 2️⃣ نظام الحجوزات المحسّن

**التحسينات:**
- ✅ إضافة حقل "عدد الأيام" (Duration Days) لكل حجز
- ✅ التحقق من صحة المدة (1-365 يوم)
- ✅ عرض عدد الأيام في تفاصيل الحجز
- ✅ حفظ المدة في قاعدة البيانات

**الاستخدام:**
1. اضغط على زر "📅 حجز موعد" من القائمة الرئيسية
2. اختر نوع الحجز (🏗️ بناء، 🔬 أبحاث، ⚔️ تدريب)
3. املأ النموذج متضمناً عدد الأيام
4. سيتم حفظ الحجز مع المدة المحددة

---

### 3️⃣ نظام التحالفات المتقدم (Advanced Alliance System)

**الأمر:**
- `/تحالفات` - فتح قائمة التحالفات

**المزايا الرئيسية:**

#### 🏰 إنشاء تحالف
- إدخال اسم التحالف (2-30 حرف)
- اختيار شعار/رمز (emoji)
- وصف التحالف (اختياري، حتى 500 حرف)
- تحديد الحد الأقصى للأعضاء (10-100)
- إضافة متطلبات الانضمام

#### 🔍 البحث عن تحالف
- عرض قائمة جميع التحالفات
- معلومات شاملة عن كل تحالف
- عدد الأعضاء والنقاط
- نوع التحالف (عام/خاص)

#### 📜 تحالفي
- عرض معلومات التحالف الخاص بك
- رتبتك في التحالف (👑 قائد، ⭐ نائب، 👤 عضو)
- إحصائيات مفصلة:
  - عدد الأعضاء
  - النقاط الكلية
  - الحجوزات النشطة والمكتملة
  - الترتيب
  - تاريخ الإنشاء

#### 🚪 مغادرة التحالف
- مغادرة التحالف الحالي
- حماية ضد مغادرة القائد (يجب نقل القيادة أولاً)
- تحديث تلقائي للإحصائيات

**قاعدة البيانات الجديدة:**
- جدول `alliance_members` - تفاصيل العضوية
- جدول `alliance_join_requests` - طلبات الانضمام
- جدول `alliance_challenges` - التحديات
- جدول `alliance_messages` - رسائل التحالف

---

### 4️⃣ لوحة تحكم الأدمن المتقدمة (Admin Panel)

**الأمر:**
- `/admin_panel` - فتح لوحة تحكم الأدمن (للمشرفين فقط)

**المزايا المتوفرة:**

#### 📊 إحصائيات البوت الشاملة
- عدد المستخدمين (المجموع والنشطون)
- عدد التحالفات وإجمالي الأعضاء
- إحصائيات الحجوزات (نشطة، مكتملة، ملغاة)
- معدل الإكمال مع شريط تقدم
- وقت التشغيل
- عدد السيرفرات
- حجم قاعدة البيانات
- أفضل 5 لاعبين

#### 🤝 إدارة التحالفات
- عرض جميع التحالفات
- معلومات مفصلة لكل تحالف
- إحصائيات شاملة
- القدرة على إدارة التحالفات

#### 💾 النسخ الاحتياطي
- إنشاء نسخة احتياطية فورية
- حفظ في مجلد `data/backups`
- عرض حجم النسخة والوقت

#### 📥 تصدير البيانات
- تصدير جميع الحجوزات إلى CSV
- ملف بترميز UTF-8 مع BOM
- يحتوي على جميع التفاصيل بما فيها عدد الأيام

#### 📜 عرض السجلات
- آخر 20 عملية في النظام
- أيقونات ملونة حسب نوع العملية
- توقيت كل عملية
- تفاصيل كل إجراء

---

### 5️⃣ نظام اللغات المتعدد (Multi-Language System)

**اللغات المدعومة:**
- 🇸🇦 العربية (افتراضي)
- 🇬🇧 الإنجليزية

**كيفية التبديل:**
1. اضغط على زر "🌐 اللغة" من القائمة الرئيسية
2. اختر اللغة المفضلة
3. سيتم حفظ اختيارك في قاعدة البيانات
4. جميع الرسائل والأزرار ستظهر باللغة المختارة

**الملفات:**
- `utils/translator.py` - نظام الترجمة
- `utils/languages/ar.json` - الترجمة العربية
- `utils/languages/en.json` - الترجمة الإنجليزية

**للمطورين:**
```python
from utils.translator import get_text

# الحصول على نص مترجم
text = get_text(user_id, 'main_menu.title')

# مع متغيرات
text = get_text(user_id, 'booking.max_bookings_msg', count=5, max=10)
```

---

### 6️⃣ مكونات UI قابلة لإعادة الاستخدام

**الملف:** `utils/ui_components.py`

**المكونات المتوفرة:**

#### NavigationButton
```python
button = NavigationButton(
    label="رجوع",
    emoji="🔙",
    style=discord.ButtonStyle.secondary,
    callback_func=my_callback
)
```

#### ConfirmView
```python
view = ConfirmView(user_id, confirm_text="✅ تأكيد", cancel_text="❌ إلغاء")
await interaction.response.send_message("هل أنت متأكد؟", view=view)
await view.wait()
if view.value:  # True إذا تم التأكيد
    # تنفيذ الإجراء
```

#### PaginationView
```python
pages = [embed1, embed2, embed3]
view = PaginationView(user_id, pages)
await interaction.response.send_message(embed=pages[0], view=view)
```

#### ProgressBar
```python
bar = ProgressBar.create(75, 100, length=10)
# النتيجة: "███████░░░ 75%"
```

#### create_colored_embed
```python
# أنواع الألوان: success, error, warning, info
embed = create_colored_embed(
    "عنوان",
    "وصف",
    color_type='success'
)
```

---

## 🗄️ تحديثات قاعدة البيانات

### جداول جديدة:
1. **alliance_members** - أعضاء التحالفات
2. **alliance_join_requests** - طلبات الانضمام
3. **alliance_challenges** - تحديات التحالفات
4. **alliance_messages** - رسائل وإعلانات التحالف

### حقول جديدة:

#### جدول `users`:
- `language` - لغة المستخدم (ar/en)

#### جدول `bookings`:
- `duration_days` - عدد أيام الحجز

#### جدول `alliances`:
- `alliance_logo` - شعار التحالف (emoji)
- `alliance_type` - نوع التحالف (public/private)
- `max_members` - الحد الأقصى للأعضاء
- `requirements` - متطلبات الانضمام
- `completed_bookings` - الحجوزات المكتملة
- `alliance_rank` - ترتيب التحالف

### تطبيق الترقية:
```bash
python database/migrations/add_new_fields.py
```

---

## 📚 الأوامر الجديدة

### للجميع:
- `/start` - القائمة الرئيسية
- `/menu` - القائمة الرئيسية
- `/تحالفات` - قائمة التحالفات

### للمشرفين فقط:
- `/admin_panel` - لوحة تحكم الأدمن

---

## 🔧 الإعدادات

### ملف `.env`:
```env
# Discord Configuration
DISCORD_BOT_TOKEN=your_bot_token_here
GUILD_ID=your_guild_id (optional)

# Role IDs
ADMIN_ROLE_ID=your_admin_role_id
MODERATOR_ROLE_ID=your_moderator_role_id

# Channel IDs
LOG_CHANNEL_ID=your_log_channel_id
ANNOUNCEMENT_CHANNEL_ID=your_announcement_channel_id

# Bot Settings
MAX_ACTIVE_BOOKINGS=5
LANGUAGE=ar
TIMEZONE=Asia/Riyadh
```

---

## 🚀 التشغيل

### التثبيت:
```bash
pip install -r requirements.txt
```

### تهيئة قاعدة البيانات:
```bash
# سيتم التهيئة تلقائياً عند أول تشغيل
python bot.py
```

### تطبيق التحديثات:
```bash
# إذا كانت قاعدة البيانات موجودة مسبقاً
python database/migrations/add_new_fields.py
```

---

## 📊 البنية المعمارية

```
bor/
├── bot.py                 # الملف الرئيسي
├── config.py              # الإعدادات
├── cogs/
│   ├── main_menu.py       # القائمة الرئيسية ✨ جديد
│   ├── bookings.py        # نظام الحجوزات (محدث)
│   ├── alliance_advanced.py  # التحالفات المتقدمة ✨ جديد
│   ├── admin_panel.py     # لوحة الأدمن ✨ جديد
│   ├── stats.py           # الإحصائيات
│   ├── admin.py           # إدارة قديمة
│   └── help.py            # المساعدة
├── database/
│   ├── db_manager.py      # مدير قاعدة البيانات (محدث)
│   ├── models.py          # النماذج (محدث)
│   ├── schema.sql         # المخطط
│   └── migrations/
│       └── add_new_fields.py  # ترقية قاعدة البيانات ✨ جديد
├── utils/
│   ├── translator.py      # نظام الترجمة ✨ جديد
│   ├── ui_components.py   # مكونات UI ✨ جديد
│   ├── languages/         # ملفات اللغات ✨ جديد
│   │   ├── ar.json
│   │   └── en.json
│   ├── embeds.py
│   ├── validators.py
│   └── permissions.py
└── tasks/
    ├── reminders_task.py
    ├── cleanup_task.py
    └── backup_task.py
```

---

## 🎯 الميزات القادمة

- [ ] نظام طلبات الانضمام للتحالفات
- [ ] تحديات ومهام جماعية للتحالفات
- [ ] إدارة متقدمة للأعضاء (ترقية، طرد، إلخ)
- [ ] نظام الإشعارات المخصص
- [ ] نظام التقييمات والتعليقات
- [ ] بحث متقدم
- [ ] نظام الإنجازات المطور
- [ ] لوحة متصدرين محسنة مع فلاتر

---

## 🛡️ الأمان

- ✅ التحقق من صلاحيات المستخدمين
- ✅ حماية البيانات الحساسة
- ✅ منع SQL Injection
- ✅ التحقق من صحة المدخلات
- ✅ نسخ احتياطية دورية
- ✅ سجلات شاملة لجميع العمليات

---

## 📞 الدعم

للإبلاغ عن مشاكل أو اقتراحات، يرجى فتح Issue على GitHub.

---

## 📝 رخصة

MIT License - انظر ملف LICENSE للتفاصيل.

---

## 👥 المساهمون

تم التطوير بواسطة فريق البوت مع مساعدة GitHub Copilot.

---

**تاريخ آخر تحديث:** 2026-02-13  
**الإصدار:** 2.0.0
